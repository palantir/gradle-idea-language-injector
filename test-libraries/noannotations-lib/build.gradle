apply plugin: 'java'
apply plugin: 'com.palantir.external-publish-jar'

group = 'com.example'
version = '1.0.0'

// Disable baseline checks for test library code
tasks.withType(JavaCompile) {
    options.errorprone.enabled = false
}
tasks.withType(Checkstyle) {
    enabled = false
}
